{
  "$defs": {
    "Preset": {
      "properties": {
        "name": {
          "title": "Name",
          "type": "string",
          "x-description": "The name of the preset.",
          "x-is-advanced-field": false,
          "x-is-configurable": true,
          "x-meta-type": "inline",
          "x-title": "Resource Preset"
        }
      },
      "required": [
        "name"
      ],
      "title": "Preset",
      "type": "object",
      "x-description": "Select the resource preset used per service replica.",
      "x-is-advanced-field": false,
      "x-is-configurable": true,
      "x-meta-type": "inline",
      "x-title": "Resource Preset",
      "x-type": "Preset"
    }
  },
  "properties": {
    "preset": {
      "$ref": "#/$defs/Preset",
      "x-description": "Select the resource preset used for the Fooocus instance. Minimal resources: 4 CPU cores, 8 GiB memory, 1 GPU with 4 GiB memory.",
      "x-title": "Fooocus preset"
    }
  },
  "required": [
    "preset"
  ],
  "title": "FooocusAppInputs",
  "type": "object"
}
